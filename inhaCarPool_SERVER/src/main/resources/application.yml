spring:
    profiles:
        active: dev
    mvc:
        pathmatch:
          matching-strategy: ant_path_matcher
    datasource:
#      url: jdbc:mysql://localhost:3306/carpool?useSSL=false&characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false
#      username: root
#      password: 1234

      url: jdbc:mysql://localhost:3306/carpool?characterEncoding=UTF-8&serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
      username: inha
      password: inha1234
      driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      properties:
        hibernate:
#          show_sql: true
          format_sql: true
      # org.hibernate.type: trace

